apiVersion: v1
kind: Service
metadata:
  name: du-service
  labels:
    app: du
spec:
  selector:
    app: du
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30000
  type: NodePort
